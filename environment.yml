name: vit-research
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      # --- Core Data Science Stack ---
      - numpy==1.26.0
      - pandas==2.2.3
      - matplotlib==3.9.2
      - scikit-learn==1.7.0
      
      # --- Deep Learning Frameworks ---
      - tensorflow==2.19.0
      - torch==2.7.0
      - torchvision==0.22.0
      
      # --- Utilities & Data Handling ---
      - requests==2.32.3
      - Pillow==11.0.0
      - tqdm==4.66.5
      - ipywidgets==8.1.5
      - ipykernel